import React, {FC, memo} from 'react';
import {Button, View} from '@core/components';
import HistoryBillListPart from '@src/screens/portrait/main/pos/parts';

type Props = {};
export const POSSellerScreen: FC<Props> = memo(({}) => {
  return (
    <View.V>
        {/*/!*<CustomerView />j*!/*/}
        {/*/!*<OperationDetail />*!/*/}
        {/*<Button.B />*/}
        {/*<Button.Submit />*/}
    </View.V>
  );
});
