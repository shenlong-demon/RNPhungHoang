import React, {FC, memo} from "react";
import {View} from "@core/components";

type Props = {};
export const HistoryBillListPart: FC<Props> = memo(({}) => {
    return <View.V>

    </View.V>;
});